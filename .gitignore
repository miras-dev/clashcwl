.coc-key
node_modules/

# Game art and the id→icon map are generated by build-assets.js, but the site
# cannot render without them — they ship. Kept small (112px, ~3.7MB total)
# since nothing renders larger than about 52px.

# build output
clashcwl-api.zip
